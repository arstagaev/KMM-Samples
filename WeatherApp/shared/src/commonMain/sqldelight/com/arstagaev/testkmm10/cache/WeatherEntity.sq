CREATE TABLE City (
    cityName TEXT NOT NULL,
    temp TEXT NOT NULL
);

insertCity:
INSERT INTO City(cityName, temp)
VALUES(?,?);

removeAllCites:
DELETE FROM City;

selectAllCityInfo:
SELECT City.*
FROM City;